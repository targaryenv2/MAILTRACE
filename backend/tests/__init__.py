"""Test package for the MailTrace backend.

Empty on purpose. ``python -m unittest discover`` needs the marker; putting
shared fixtures here would run them on import for every module that touches the
package, which is how a test suite acquires order dependence.
"""
